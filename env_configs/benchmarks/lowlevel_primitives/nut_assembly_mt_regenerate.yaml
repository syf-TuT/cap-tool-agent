# Low-level primitive benchmark: nut_assembly, multiturn full-code regeneration baseline.

env:
  _target_: capx.envs.tasks.franka.franka_nut_assembly.FrankaNutAssemblyCodeEnv
  cfg:
    _target_: capx.envs.tasks.base.CodeExecEnvConfig
    low_level: franka_robosuite_nut_assembly_low_level
    privileged: true
    apis:
      - FrankaControlNutAssemblyPrivilegedApi
    prompt: |
      You are controlling a Franka Emika robot with API described below.
      Goal: grasp and insert the `brown square nut` onto the `brown square block`.
      Note that you would grasp the nut by its handle. You can try language query `extruded handle of the brown square nut` to get a good grasp at the handle.
      The brown square nut and the extruded handle of the brown square nut are part of the same rigid body.
      The grasp pose query for 'extruded handle of the brown square nut' returns an end-effector pose expressed in world frame, located on the handle region.
      The nut's object center pose obtained via 'white hollow center of the brown square nut' and the handle grasp pose obtained via 'extruded handle of the brown square nut' have a fixed rigid transform, which must be applied correctly when inserting the nut onto the peg.
      You may write Python comments for reasoning but ONLY write executable Python code and do not write it in code fences.
      The functions below are already imported. If you want to use numpy or scipy for spatial transformations, import it explicitly.
      After this code executes, you will receive stdout/stderr and may regenerate the remaining code.
    multi_turn_prompt: |
      The following code blocks were just executed:
      ```python
      {executed_code}
      ```
      The current console stdout is:
      ```
      {console_stdout}
      ```
      The current console stderr is:
      ```
      {console_stderr}
      ```
      If there were errors, regenerate the remaining executable Python code.
      If the task is complete, respond with FINISH.
      Please respond with EXACTLY ONE of the following:
      - The word 'REGENERATE' followed immediately by new Python code in a fenced code block (```python...```) if you want to modify the remaining code.
      - The word 'FINISH' if the task appears to be complete.

api_servers:
  - _target_: capx.serving.launch_pyroki_server.main
    port: 8116
    host: 127.0.0.1
    robot: panda_description
    target_link: panda_hand

record_video: true
use_visual_feedback: false
use_img_differencing: false
use_video_differencing: false
output_dir: ./outputs/bench_lowlevel/nut_assembly/mt_regenerate

use_oracle_code: false
trials: 1
num_workers: 1
